import React from "react";
import "../styles/App.sass";

export default function() {
  return (
    <header className="App-header">
      <p className="App-heading"> Where in the World ? </p>
      <div className="Heading-right">
        {/* <a className="App-sub-heading" href="#">
          Dark Mode
        </a> */}
      </div>
    </header>
  );
}
